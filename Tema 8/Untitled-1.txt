//Dos formas de pasar de int a string
Integer.toString(int);
String.valueOf(int);

//Pasar string a int
String numero= "1";
int parse_numero = Integer.parseInt(numero);

//Juntar dos arrays en otro array
String[] both = (String[])ArrayUtils.addAll(first, second);